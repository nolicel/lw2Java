//Реализуйте метод, который принимает на вход строку и возвращает true,
// если строка заканчивается на «ed» и false в противном случае;
public class Ed_Finder {
    public static boolean isEd (String input) {
        return input.endsWith("ed");
    }
}
